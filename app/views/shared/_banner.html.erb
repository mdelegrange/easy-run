<div class="banner" style="background-image: linear-gradient(to right, rgba(10, 207, 254, 0.7) 0%, rgba(73, 90, 255, 0.7) 100%), url(<%= image_path 'home-run.jpg'%>);">
  <div class="banner-content">
    <h1 class="home-title">EASY RUN</h1>
    <h2 class="home-subtitle">Find your Marathon and run it.</h2>
    <br>
        <div class="input-group input-group-lg search-btn">
          <%= form_tag races_path, method: :get do %>
          <%= select_tag "department" , options_for_select(@departments_options), class: 'form-control-home' %>
          <span><div class="btn-home">
          <%= submit_tag "RUN", :class => "btn btn-home-design
          btn-lg" %></div></span>
        </div>
        <%end%>
  </div>
</div>

      